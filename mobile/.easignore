# Exclude generated native directories - EAS will run expo prebuild
android/
ios/

# Exclude node_modules - EAS installs them on its own build servers
node_modules/

# Exclude local build outputs
mobile/android/
mobile/ios/
mobile/node_modules/
